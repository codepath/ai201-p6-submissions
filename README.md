# AI201 P6 — CineLog calibration (branch: `maya-commits`)

This branch holds the **maya** submission under [`maya/`](maya/) — including its
source, tests, `pr-response.md`, `CALIBRATION_NOTES.md`, and this branch's real commit history.

The sibling folders (`derek/`, `priya/`, `jordan/`, `tyler/`) are **stubs** — each of those
submissions lives on its own `<persona>-commits` branch (see the stub `README.txt` in each).

For the full persona index and how to point the grader at each branch, see the `main` branch.
