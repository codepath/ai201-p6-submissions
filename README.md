# AI201 P6 — CineLog calibration (branch: `tyler-commits`)

This branch holds the **tyler** submission under [`tyler/`](tyler/) — including its
source, tests, `pr-response.md`, `CALIBRATION_NOTES.md`, and this branch's real commit history.

The sibling folders are **stubs** — each of those submissions lives on its own
`<persona>-commits` branch (see the stub `README.txt` in each).

For the full persona index and how to point the grader at each branch, see the `main` branch.
